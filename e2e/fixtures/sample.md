# answerLoops Test Doc

## Getting Started

To configure the client connection, set the `DATABASE_URL` environment variable.

## Authentication

Use OAuth2 with your provider of choice. Set `AUTH_SECRET` to a random string.

## Troubleshooting

If the bot is not responding, check that `BOT_SECRET` matches the integration setting.
